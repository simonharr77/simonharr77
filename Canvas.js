function setup(){
   var canvas= createCanvas (800, 800)
   canvas.parent("myContainer");
}
function draw(){
  var text = createDiv('This is an HTML string!');
    text.position(50,50);
    if(mouseIsPressed)
    background(r,g,b)
    frameRate(30)
    {
{
      r = random(255);
      g = random(255);
      b = random(255);
      l = random(100);
      w = random(100);
}
{
fill(r, g, b);

rect(mouseX,mouseY, l, w);
ellipse(mouseY,mouseX, l, w);

  }
{
  let r = 255;
  let g = 255;
  let b = 255;
}
}
    }
